package com.syntax.class18;

public class recap {
    public static void main(String[] args) {
        //variables types
        //local,instance,static
        //local variables:any variables that is declared
        // inside a method or a block like loop for loop if condition
        //or inside the constructor are called local variables
        //properties of local variables.we can access them outside of the block.
        //as sson as we are done with this, local var are destroyed.
        //instance variables:all of the variables which are declared outside of the matters
        //or blocks or constructors.but inside a class are called instance variables.
        //properties of instance;
    }
}
